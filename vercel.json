{
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": "packages/web-ui/dist",
  "routes": [
    { "src": "/v1/chat/completions",  "dest": "/api/openai/chat"   },
    { "src": "/v1/images/generations","dest": "/api/openai/images" }
  ],
  "env": {}
}